name: timer
description: Simple timer application with a beautiful design.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # functional programming to dart
  dartz: ^0.9.2
  # for picking the timer date
  flutter_duration_picker: ^1.0.4
  # the background
  wave: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  